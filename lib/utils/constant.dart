//this pg contains the constant things


//here we have a image which will be used in case when lets say API fails to load some image then by default this image would show
class Constant {
  static String imgURL = '';
}
